version: '3.9' # docker 버전

services:
  mariadb:
    image: debian:bullseye
    restart: always
    expose:
      - "3306"
    networks:
      - intra
    tty: true

volumes:
  db_data: {}

networks:
  intra:
    driver: bridge